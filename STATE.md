# Loop state
- 2026-09-10 late: build 18 (Groups) LIVE for mates. Build 19 SHIPPED (uploaded, in mates review): feed pills never wrap + ViewThatFits row (fixes Lucas/Menno alignment screenshots), whole-unit time labels, Groups review fix round (8 items), member displayName.
- 2026-09-11 pm 2: Tim (device): photos mirrored + must not be screenshot-able; "is de feed opgeschoond?". Done: front-camera capture mirrored to match the preview (CameraView); photo viewer renders inside ScreenshotShield (secure UITextField canvas → black in screenshots/recordings; receipts still fire); prod: 6 stale 24 h drinks clamped via tools/clamp-lifetime.mjs --yes. Build 22 = these. Device-only: both fixes are ASSUMED until Tim confirms (rig cannot screenshot-test a shield or use a camera).
- 2026-09-11 pm: Tim delegated the two design calls. Icon A (pint on amber) installed; dark mode lifted to grey (ground #151517, surface #262629; lists own their ground, grouped sections paint surface, Home feed rows clear). Rig green light+dark (run 34599019715). Build 21 uploaded, attached to Mates, beta review submitted (tools/ship-build.sh 21).
- OPEN: (d) Menno's sign-up error text. (e) Tim's "circles" screenshot. (f) tighten rules expiry cap 25 h → 3 h once mates are on build 20+.
- 2026-09-11 late am: privacy policy live at https://beerwithme-prod.web.app/privacy (hosting cleanUrls on; deploy.sh --hosting); deleteAccount now leaves groups (emu test); three app icon candidates sent to Tim (tools/icon/candidates.js renders SVG via resvg in a scratch npm dir). Needs Tim: icon pick.
- 2026-09-11 am: Tim's three asks shipped as build 20 (ios-testflight run 34582343354; attach + review via tools/ship-build.sh 20): one live drink per person (feed + map show each mate's newest; server deletes older ones on a new log), 2 h lifetime (client + server clamp; rules cap still 25 h so build 19 can log, tighten to ~3 h once mates are on 20), picker glasses rest empty, changeable nickname (Settings row + changeDisplayName callable mirroring group members). Functions deployed. Gates: BeerKit 48, functions 6, emu 85. Rig green (run 34583003479) with the nickname sheet frame. Build 20 attached to Mates + beta review submitted.
- 2026-09-10 late 2: seeded rig FIXED (run 34531493930 green, all 5 populated frames light+dark). Cause: the Auth emulator maps every API-key request to its `--project` (demo-pubdates), so the seed's beerwithme-prod accounts were invisible; seed now creates accounts in demo-pubdates (Firestore/Storage stay beerwithme-prod). Push index READY + cooldown query live in prod (handover item 2 closed). Groups verdict pass on populated frames: one fix (leaderboard separators pinned to the name; the crown row and #n rows differed).
- HANDOVER: docs/HANDOVER_2026-09-10.md (everything open + the one big plan). Read it first.
- 2026-09-10 night: build 17 (pour→camera, universal invite links, 15-min glass, cooldown, soft drink out) and build 18 (Groups tab + leaderboard, join links) shipped; both submitted to mates. Milestone G done except the design review pass; universal-link AASA fixed (hosting ignore).
- Current task: 12 (handover; compile loop now lives in CI)
- Done: 1-8, 9, 10 (all screens compiled on GitHub macOS runner, run 33852266356), 11 (report/block wired; docs/privacy-policy.md, docs/app-store-checklist.md)
- Infra done 2026-09-04: `ios-build` workflow (unsigned simulator compile on every App/BeerKit push), `ios-testflight` workflow (manual, cloud-managed signing), iOS app registered in beerwithme-prod, GoogleService-Info.plist in App/Resources (gitignored) + GitHub secret FIREBASE_PLIST_B64. All 7 functions deployed in europe-west4 and matching source.
- Parked: —
- 2026-09-07: build 1 (Pints With Mates, com.timjansen.BeerWithFriends) uploaded to TestFlight via ios-testflight, IN_BETA_TESTING, Tim is internal tester. Signing: manual cert+profile from the ASC API (secrets DIST_*).
- 2026-09-07: Firebase Auth initialized (Identity Platform, free tier) + Apple provider enabled via API.
- 2026-09-07: app renamed to **PubDates** (display name, UI strings, docs, notify title). Bundle id, Xcode target/scheme, Firebase project and provisioning profile name unchanged on purpose.
- 2026-09-07: build 2 (optimistic logging, resilient feed, push token fetch, offline-safe sign-out, profile retry, camera guard) uploaded to TestFlight.
- 2026-09-07 pm: Milestone C started. PRODUCT.md + docs/design/direction.md written; all six screens redesigned (Opus workers) on Theme.swift; compiled clean. Screenshot rig (ios-test workflow: simulator + emulators, light/dark PNGs) being stabilised.
- 2026-09-07: build 3 (redesign, launch screen fix, bundle name) uploaded to TestFlight. Screenshot rig green light+dark.
- 2026-09-07 late: finish review (Fable) → fix round (3 Opus workers) → verdict ship; DESIGN.md written. QR add-a-mate (QRMateView, pubdates://add/<u>, sticky AppState inbox). Build 4 uploaded. tools/reset-prod.sh for Tim to wipe prod data.
- 2026-09-07 eve: photos failed in prod (functions SA lacks iam.serviceAccounts.signBlob; VM SA cannot grant it). Fixed by delivering photo bytes in the callable (no signed URL). Build 5 shipped. Backlog from Tim: location on a beer (opt-in place name), quick replies to a beer push.
- 2026-09-07 night: push delivery goes straight to APNs (functions/src/apns.ts, key in Secret Manager APNS_KEY, read at runtime); build 6 uploaded. External TestFlight group 'Mates' with public link https://testflight.apple.com/join/1QfbyRt9; build 8 submitted for beta review (WAITING_FOR_REVIEW). tools/test-push.mjs for a real delivery check.
- 2026-09-07 19:03: PUSH WORKS end to end (build 11: FirebaseAppDelegateProxyEnabled=false + Messaging.apnsToken fallback; test push 200 from tools/test-push.mjs, received on Tim's phone).
- 2026-09-09: Apple beta review approved builds 8 and 11; public link https://testflight.apple.com/join/1QfbyRt9 live with build 11. Tim can invite mates.
- 2026-09-09: place feature shipped (build 12): opt-in Settings toggle, LocationPlaceProvider (POI/city, 3 s), rules allow optional place ≤60, push title 'at <place>'. Submitted for beta review.
- Needs Tim: try build 12 with the toggle on; invite mates; report device feedback.
- 2026-09-09 eve: quick replies shipped (build 13): BEER notification category with Cheers/On my way/Jealous actions, beers/{id}/replies rules, onReplyCreated mirror + push, ReactionInbox, context-menu replies, reply pills. First external tester joined via the link.
- 2026-09-10: drinks (7 drawn glasses, pour + 24 h drain), Map tab (bar coordinates, opt-in), change username (callable, once/day) shipped. Finish review (Fable) → fix round (3 Opus workers) → 2 finish tweaks; DESIGN.md rewritten. Build 15 to mates after review.
- Open: Menno's sign-up error (text unknown); rig seed (mate + photo + located drink) for photo viewer, chips, map pin, username sheet captures; app icon still a placeholder. run tools/reset-prod.sh himself (classifier blocks Claude from wiping prod). APNs key. rename the App Store Connect record to PubDates (App Information, Name) so the title is reserved and matches the next build. APNs key (RUNBOOK step 3.1, upload the .p8 to tim-server and give me the Key ID). Then step 5 (external testers).
- Next code work: fix review findings 1-5 + CameraView crash (see session 2026-09-04 review), then item 6-10.
- Mac note (historical): the Mac's Command Line Tools are corrupted (see README). No longer on the critical path; nothing requires a Mac.
